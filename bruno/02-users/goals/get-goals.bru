meta {
  name: Get goals
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/users/{{userId}}/goals
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
