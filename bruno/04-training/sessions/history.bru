meta {
  name: Session history
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/training/sessions/history?limit=20
  body: none
  auth: bearer
}

params:query {
  limit: 20
}

auth:bearer {
  token: {{token}}
}
