meta {
  name: List reminders
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/engagement/reminders?status=pending
  body: none
  auth: bearer
}

params:query {
  status: pending
}

auth:bearer {
  token: {{token}}
}

docs {
  status: pending | completed | all
}
