meta {
  name: Create meal plan
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/nutrition/meal-plans
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "nombre": "Plan semanal Bruno test",
    "descripcion": "Generado desde Bruno"
  }
}
