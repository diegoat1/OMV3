meta {
  name: Get table contents
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/admin/database/tables/REPLACE_TABLE_NAME?limit=100&offset=0
  body: none
  auth: bearer
}

params:query {
  limit: 100
  offset: 0
}

auth:bearer {
  token: {{token}}
}
