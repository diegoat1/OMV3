meta {
  name: Dashboard
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/analytics/dashboard
  body: none
  auth: bearer
}

params:query {
  ~user:
  ~patient_id:
}

auth:bearer {
  token: {{token}}
}
